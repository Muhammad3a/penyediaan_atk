[phases.setup]
nixPkgs = ['php', 'composer', 'nodejs', 'npm']

[phases.build]
cmds = [
  "mkdir -p bootstrap/cache",           # ⬅️ Tambah baris ini
  "composer install --ignore-platform-reqs",
  "npm ci",
  "npm run build"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"
